# Parches

- `work/`: candidatos no habilitados por defecto y aptos para conservar en Git.
- `final/`: parche final validado, ligado al hash exacto y con
  `is_enabled = false`.

Las copias habilitadas durante pruebas pertenecen a
`_local/xenia/patches-enabled/` y nunca se versionan.
